import { createReactIcon } from '../IconBase'

/**
 * {{ componentName }} 图标
 */
export const {{ componentName }}Icon = createReactIcon(
  '{{ componentName }}',
  {{#if isMultiPath}}
  [
    {{#each paths}}
    '{{ this }}'{{#unless @last}},{{/unless}}
    {{/each}}
  ]
  {{else}}
  '{{ paths.[0] }}'
  {{/if}},
  '{{ viewBox }}'
)




